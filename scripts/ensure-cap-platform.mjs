import fs from 'node:fs';
import path from 'node:path';
import { execFileSync } from 'node:child_process';

const platform = process.argv[2];
const allowed = new Set(['android', 'ios']);

if (!allowed.has(platform)) {
  console.error('Usage: node scripts/ensure-cap-platform.mjs <android|ios>');
  process.exit(2);
}

if (fs.existsSync(platform)) {
  console.log(`Capacitor platform already exists: ${platform}`);
} else {
  console.log(`Adding Capacitor platform: ${platform}`);
  execFileSync('npx', ['cap', 'add', platform], { stdio: 'inherit' });
}

if (platform === 'android') {
  const root = new URL('../', import.meta.url).pathname;
  const javaDir = path.join(root, 'android', 'app', 'src', 'main', 'java', 'com', 'salliha', 'app');
  fs.mkdirSync(javaDir, { recursive: true });
  fs.writeFileSync(path.join(javaDir, 'CustomAdhanSoundPlugin.java'), "package com.salliha.app;\n\nimport android.content.ContentResolver;\nimport android.content.ContentValues;\nimport android.media.AudioAttributes;\nimport android.net.Uri;\nimport android.os.Build;\nimport android.provider.MediaStore;\nimport android.util.Base64;\nimport androidx.annotation.NonNull;\nimport com.getcapacitor.JSObject;\nimport com.getcapacitor.Plugin;\nimport com.getcapacitor.PluginCall;\nimport com.getcapacitor.annotation.CapacitorPlugin;\nimport com.getcapacitor.annotation.PluginMethod;\nimport android.app.NotificationChannel;\nimport android.app.NotificationManager;\n\n@CapacitorPlugin(name = \"CustomAdhanSound\")\npublic class CustomAdhanSoundPlugin extends Plugin {\n  private static final String PREFS = \"salliha_custom_adhan\";\n\n  @PluginMethod\n  public void save(PluginCall call) {\n    String name = call.getString(\"name\", \"salliha-adhan.mp3\");\n    String base64 = call.getString(\"base64\", \"\");\n    String mime = call.getString(\"mimeType\", \"audio/mpeg\");\n    if (base64.isEmpty()) { call.reject(\"ملف الأذان فارغ.\"); return; }\n\n    try {\n      byte[] bytes = Base64.decode(base64, Base64.DEFAULT);\n      ContentValues values = new ContentValues();\n      values.put(MediaStore.Audio.Media.DISPLAY_NAME, \"Salliha_\" + sanitize(name));\n      values.put(MediaStore.Audio.Media.MIME_TYPE, mime);\n      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {\n        values.put(MediaStore.Audio.Media.RELATIVE_PATH, \"Music/Salliha\");\n        values.put(MediaStore.Audio.Media.IS_PENDING, 1);\n      }\n      ContentResolver resolver = getContext().getContentResolver();\n      Uri uri = resolver.insert(MediaStore.Audio.Media.EXTERNAL_CONTENT_URI, values);\n      if (uri == null) { call.reject(\"تعذر حفظ ملف الأذان.\"); return; }\n      try (java.io.OutputStream out = resolver.openOutputStream(uri)) {\n        if (out == null) throw new java.io.IOException(\"output unavailable\");\n        out.write(bytes);\n      }\n      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {\n        ContentValues done = new ContentValues();\n        done.put(MediaStore.Audio.Media.IS_PENDING, 0);\n        resolver.update(uri, done, null, null);\n      }\n      getContext().getSharedPreferences(PREFS, 0).edit().putString(\"uri\", uri.toString()).putString(\"name\", name).apply();\n      configureChannels(uri);\n      JSObject result = new JSObject();\n      result.put(\"uri\", uri.toString());\n      call.resolve(result);\n    } catch (Exception e) {\n      call.reject(\"تعذر حفظ ملف الأذان: \" + e.getMessage());\n    }\n  }\n\n  @PluginMethod\n  public void get(PluginCall call) {\n    String uri = getContext().getSharedPreferences(PREFS, 0).getString(\"uri\", \"\");\n    String name = getContext().getSharedPreferences(PREFS, 0).getString(\"name\", \"\");\n    JSObject result = new JSObject();\n    result.put(\"uri\", uri);\n    result.put(\"name\", name);\n    call.resolve(result);\n  }\n\n  @PluginMethod\n  public void remove(PluginCall call) {\n    String uri = getContext().getSharedPreferences(PREFS, 0).getString(\"uri\", \"\");\n    if (!uri.isEmpty()) {\n      try { getContext().getContentResolver().delete(Uri.parse(uri), null, null); } catch (Exception ignored) {}\n    }\n    getContext().getSharedPreferences(PREFS, 0).edit().clear().apply();\n    call.resolve();\n  }\n\n  @PluginMethod\n  public void configureChannels(PluginCall call) {\n    String uri = call.getString(\"uri\", \"\");\n    if (!uri.isEmpty()) configureChannels(Uri.parse(uri));\n    call.resolve();\n  }\n\n  private void configureChannels(Uri soundUri) {\n    NotificationManager manager = (NotificationManager) getContext().getSystemService(android.content.Context.NOTIFICATION_SERVICE);\n    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.O || manager == null) return;\n    AudioAttributes attrs = new AudioAttributes.Builder()\n      .setUsage(AudioAttributes.USAGE_NOTIFICATION)\n      .setContentType(AudioAttributes.CONTENT_TYPE_SONIFICATION)\n      .build();\n    manager.deleteNotificationChannel(\"adhan-v3-custom-normal\");
    manager.deleteNotificationChannel(\"adhan-v3-custom-fajr\");
    NotificationChannel normal = new NotificationChannel(\"adhan-v3-custom-normal\", \"أذان الصلاة — صوتي\", NotificationManager.IMPORTANCE_HIGH);\n    normal.setSound(soundUri, attrs);\n    NotificationChannel fajr = new NotificationChannel(\"adhan-v3-custom-fajr\", \"أذان الفجر — صوتي\", NotificationManager.IMPORTANCE_HIGH);\n    fajr.setSound(soundUri, attrs);\n    manager.createNotificationChannel(normal);\n    manager.createNotificationChannel(fajr);\n  }\n\n  private String sanitize(String value) {\n    return value.replaceAll(\"[^a-zA-Z0-9._-]\", \"_\");\n  }\n}\n");
  fs.writeFileSync(path.join(javaDir, 'MainActivity.java'), "package com.salliha.app;\n\nimport com.getcapacitor.BridgeActivity;\nimport com.getcapacitor.Plugin;\nimport java.util.Arrays;\n\npublic class MainActivity extends BridgeActivity {\n  @Override\n  public void onCreate(android.os.Bundle savedInstanceState) {\n    registerPlugin(CustomAdhanSoundPlugin.class);\n    super.onCreate(savedInstanceState);\n  }\n}\n");
  console.log('Prepared Android custom adhan sound plugin.');
}
